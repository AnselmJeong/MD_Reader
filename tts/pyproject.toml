[project]
name = "md-reader-tts"
version = "0.1.0"
description = "MD Reader local NeuTTS sidecar"
requires-python = ">=3.11,<3.13"
dependencies = [
  "neutts[llama,onnx]",
  "phonemizer>=3.0.0",
  "pyarrow<21",
  "pyaudio",
]

[tool.uv]
package = false
